Place the official DGII e-CF XSD files in this directory.

The certification XML validator currently performs mandatory well-formed XML
validation and reports XSD validation as unavailable until the official schemas
and a runtime XSD validation engine are configured.
